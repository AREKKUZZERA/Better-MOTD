<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>bettermotd</groupId>
  <artifactId>BetterMOTD</artifactId>
  <version>1.8.0</version>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.11.3</junit.version>

    <minecraft.target>mc-1.21.x</minecraft.target>
    <plugin.api.version>1.21</plugin.api.version>

    <!-- Default artifact targets Minecraft 1.21.x. Use -Pmc-26.1.x for the 26.1.x artifact. -->
    <paper.api.version>1.21-R0.1-SNAPSHOT</paper.api.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>${paper.api.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}-${project.version}-${minecraft.target}</finalName>

    <resources>
      <!-- Filter ONLY the config files that may contain ${project.version}, etc -->
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>plugin.yml</include>
          <include>config.yml</include>
          <include>config.schema.json</include>
        </includes>
      </resource>

      <!-- Copy the rest without filtering -->
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes>
          <exclude>plugin.yml</exclude>
          <exclude>config.yml</exclude>
          <exclude>config.schema.json</exclude>
        </excludes>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <encoding>${project.build.sourceEncoding}</encoding>

          <!-- Disable annotation processing to silence "No processor claimed ..." -->
          <proc>none</proc>

          <compilerArgs>
            <arg>-Xlint:all</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
      </plugin>

      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>2.44.0</version>
        <configuration>
          <java>
            <palantirJavaFormat>
              <version>2.90.0</version>
            </palantirJavaFormat>
          </java>
        </configuration>
        <executions>
          <!-- Run formatting check during "verify" (CI-friendly) -->
          <execution>
            <id>spotless-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>package-mc-26.1.x</id>
            <phase>package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <property name="variant.dir" value="${project.build.directory}/variant-mc-26.1.x"/>
                <delete dir="${variant.dir}"/>
                <mkdir dir="${variant.dir}"/>
                <copy todir="${variant.dir}">
                  <fileset dir="${project.build.outputDirectory}"/>
                </copy>
                <copy file="${project.basedir}/src/main/variants/mc-26.1.x/plugin.yml"
                      tofile="${variant.dir}/plugin.yml"
                      overwrite="true">
                  <filterset>
                    <filter token="PROJECT_VERSION" value="${project.version}"/>
                  </filterset>
                </copy>
                <jar destfile="${project.build.directory}/${project.artifactId}-${project.version}-mc-26.1-26.1.2.jar"
                     basedir="${variant.dir}"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>mc-1.21.x</id>
      <properties>
        <minecraft.target>mc-1.21.x</minecraft.target>
        <plugin.api.version>1.21</plugin.api.version>
        <paper.api.version>1.21-R0.1-SNAPSHOT</paper.api.version>
      </properties>
    </profile>

    <profile>
      <id>mc-26.1.x</id>
      <properties>
        <minecraft.target>mc-26.1-26.1.2</minecraft.target>
        <plugin.api.version>26.1</plugin.api.version>
        <paper.api.version>26.1.2.build.63-stable</paper.api.version>
      </properties>
    </profile>
  </profiles>
</project>
